[project]
name = "original-paper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "lightning>=2.6.1",
    "torch>=2.11.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
